name: test config for body
destinations:
  test-api: http://localhost:3000

testcases:
  - target: /echo/json
    setting:
      request:
        method: POST
        headers:
          content-type: application/json # TODO auto ?
        body:
          foo: hoge
          bar: fuga
          baz: piyo
      evaluate:
        body:
          json:
            patch:
              - op: test
                path: ""
                value:
                  foo: hoge
                  bar: fuga
                  baz: piyo
