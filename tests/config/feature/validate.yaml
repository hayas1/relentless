name: test config for validate
destinations:
  test-api: http://localhost:3000

testcases:
  - target: /echo/json?foo=hoge&bar=fuga&baz=piyo
    setting:
      evaluate:
        body:
          json:
            patch:
              - op: test
                path: ""
                value:
                  foo: hoge
                  bar: fuga
                  baz: piyo
