name: test config for record
destinations:
  test-api: http://localhost:3000

testcases:
  - target: /echo/
  - target: /echo/body
    setting:
      http:
        request:
          method: POST
          body: hello world
        evaluate:
          body:
            plaintext:
              regex: hello world
  - target: /health/rich
