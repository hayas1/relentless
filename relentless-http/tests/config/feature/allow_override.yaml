name: test config for allow override
destinations:
  test-api: http://localhost:3000
setting:
  allow: true

testcases:
  - target: /health/disabled
  - target: /health/disabled
  - target: /health/disabled
    setting:
      allow: false
